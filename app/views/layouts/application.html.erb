<!DOCTYPE html>
<html ng-app="quiz">
<head>
  <title>QuizApp</title>
  <%= stylesheet_link_tag    "screen", media: 'screen' %>
  <%= javascript_include_tag "angular.min.js" %>
  <%= javascript_include_tag "angular-resource.min.js" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <title><%= yield(:title) %></title>
</head>
<body>

<div id="Container">
  <% if signed_in? %>
    <header class="logged-in" ng-controller="SessionCtrl">
      <span class="welcome">Hi, <%= @current_user.email %></span><%=link_to "Logout", logout_path, method: "delete" %></li>
    </header>
  <% else %>
    <header>
      <ol>
        <li><%=link_to "Login", login_path %></li>
        <li><%=link_to "Register", register_path %></li>
      </ol>
    </header>
  <% end %>
  </header>
  <%= yield %>
  <%= debug(params) if Rails.env.development? %>
</div>

</body>
</html>
